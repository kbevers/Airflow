# Airflow

```
$ git clone https://github.com/Kortforsyningen/AirFlow.git
$ cd AirFlow
$ conda install mamba -n base -c conda-forge
$ mamba env create -f environment.yaml
$ conda activate airflow
```

